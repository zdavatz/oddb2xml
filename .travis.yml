language: ruby

bundler_args: --without debugger

before_install:
  - gem update --system 2.1.11
  - gem --version

script: bundle exec rspec

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1
  - ruby-head
  
matrix:
  allow_failures:
    - rvm: ruby-head

notifications:
  email:
    recipients:
      - ngiger@ywesee.com
      - yasaka@ywesee.com
      - zdavatz@ywesee.com
